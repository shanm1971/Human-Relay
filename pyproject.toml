[project]
name = "human-relay"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn", "sqlalchemy", "psycopg[binary]", "pydantic", "pyjwt[crypto]", "jsonschema", "httpx", "mcp"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

